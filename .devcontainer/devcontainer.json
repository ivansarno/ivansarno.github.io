{
    "name": "IVANSARNO.GITHUB.IO",
    "build": {
    "dockerfile": "Dockerfile"
    },
    "features": {},
    "postCreateCommand": "echo 'Ready!'",
    "customizations": {
    "vscode": {
        "extensions": [
            "ltex-plus.vscode-ltex-plus",
            "DavidAnson.vscode-markdownlint"
        ]
    }
    },
    "mounts": [],
    "remoteUser": "root"
    "postCreateCommand": "git config --global user.name 'Ivan Sarno' && git config --global user.email 'ivansarno@outlook.com'"
  }